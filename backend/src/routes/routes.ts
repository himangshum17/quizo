const BASE_ENDPOINT_PREFIX = "/api/v1";

const ROUTES = {
  AUTH: `${BASE_ENDPOINT_PREFIX}/auth`,
  CATEGORIES: `${BASE_ENDPOINT_PREFIX}/categories`,
  USER: `${BASE_ENDPOINT_PREFIX}/user`,
};

export { ROUTES };
